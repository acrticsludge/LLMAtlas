# v0.1.9

## What's Changed

### 🐛 Fixes
- Auto-detect now reads from `.opencode/opencode.old.json` (was renamed)
- Auto-detect now loads `.env` files (project `.env` and `.opencode/.env`)
- Better error message telling user exactly which env var to set

### 🧹 Chores
- Bump 0.1.8 → 0.1.9
